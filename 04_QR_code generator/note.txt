inquires package to get the user input
use the qr-image to turn the iser enterd url into a qr code image 
now save the the user input using the native fs module 

https://www.npmjs.com/package/qr-image
https://www.npmjs.com/package/inquirer
